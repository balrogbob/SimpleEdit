# ============================================================================
# SimpleEdit - Requirements File
# ============================================================================
# 
# This file specifies all external dependencies for SimpleEdit.
# 
# Required packages (always needed):
#   - tkinter: included with Python, no pip needed
#   
# Optional packages:
#   - torch, tiktoken: for AI autocomplete feature (GPT-2 code suggestions)
#     Install manually if needed: pip install torch tiktoken
#
# PyInstaller dependencies (for building Windows .exe):
#   - pyinstaller: bundles Python code into executable
#   - See WINDOWS-BUILD.md for detailed build instructions
#
# ============================================================================

# --- CORE DEPENDENCIES (AI Feature) ---
# Optional: Machine learning model inference and tokenization
# Install with: pip install torch tiktoken
# These are NOT required for basic editor functionality

torch==2.5.1
tiktoken==0.8.0

# --- PYINSTALLER (Building Windows .exe) ---
# Required only when building standalone executables
# See WINDOWS-BUILD.md for build instructions

pyinstaller==6.11.1
pyinstaller-hooks-contrib==2024.10
altgraph==0.17.4
pywin32-ctypes==0.2.3
pefile==2023.2.7

# --- INDIRECT DEPENDENCIES ---
# These are automatically installed by the packages above
# Listed for reference and build reproducibility

certifi==2024.8.30
charset-normalizer==3.4.0
colorama==0.4.6
Cython==3.0.11
filelock==3.16.1
fsspec==2024.10.0
idna==3.10
Jinja2==3.1.4
MarkupSafe==3.0.2
mpmath==1.3.0
networkx==3.4.2
numpy==2.1.3
packaging==24.2
regex==2024.11.6
requests==2.32.3
setuptools==65.5.0
sympy==1.13.1
tqdm==4.67.1
typing_extensions==4.12.2
urllib3==2.2.3

# NOTE: pip==23.0.1 and related build tools are typically managed separately
# and not required to be in requirements.txt
